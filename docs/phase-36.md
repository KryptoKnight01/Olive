# Phase 36 — Native Python Strategy Engine

The native strategy evaluator is deterministic and versioned. Pine and Python signal histories
can be compared with an explicit parity percentage before any authority change is considered.
