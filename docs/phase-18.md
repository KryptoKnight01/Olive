# Phase 18 — Admin Web Application V1

The first administrative interface exposes an API-backed Command Center with health, positions, orders, signals, active emergency controls, and risk-summary fields. It reads authoritative ledgers rather than browser state. Configuration mutation and security controls remain governed by the following phases.

Migration `20260825_0019` records the API baseline without duplicating operational data.

Phase 19 adds controlled configuration changes.
